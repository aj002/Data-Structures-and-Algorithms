#include<stdio.h>

#define max_size 100

struct stack
{
    int aSize;
    int nStack;
    int a[max_size];
    int top[max_size];
    int next[max_size];
    int nextAvailable;
};

int main()
{
    return 0;
}